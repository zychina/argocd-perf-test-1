apiVersion: v1
kind: Service
metadata:
  name: perf-test1-app272-svc
  namespace: clawtest
  labels:
    app: perf-test1-app272
spec:
  selector:
    app: perf-test1-app272
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
