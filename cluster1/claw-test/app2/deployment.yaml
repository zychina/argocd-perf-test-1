apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: clawtest
  labels:
    app: app2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app2
        image: anolis-registry.cn-hongkong.cr.aliyuncs.com/openanolis/nginx:1.14.1-8.6
        ports:
        - containerPort: 80
        volumeMounts:
        - name: data-vol
          mountPath: /data
        - name: log-vol
          mountPath: /var/log/app
      volumes:
      - name: data-vol
        persistentVolumeClaim:
          claimName: app2-data-pvc
      - name: log-vol
        persistentVolumeClaim:
          claimName: app2-log-pvc
